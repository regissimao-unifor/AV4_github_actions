{
  "name": "supabase-frontend",
  "version": "1.0.0",
  "description": "Static product CRUD frontend with Cypress UI tests (backend mocked via cy.intercept).",
  "scripts": {
    "start": "http-server -p 8080 -c-1 .",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "test": "start-server-and-test start http://localhost:8080 cy:run"
  },
  "devDependencies": {
    "cypress": "^13.13.0",
    "http-server": "^14.1.1",
    "start-server-and-test": "^2.0.4"
  }
}
